请帮我构建一个**模块化、工程化**的 A 股超短线量化交易项目，使用深度学习模型预测次日涨幅，实现今日买入明日卖出，每日推荐10只股票。

要求：
1. 项目采用模块化架构，不要写在单个文件里，按功能拆分为多个 .py 文件。
2. 使用 tushare 获取历史日线数据，token 由我填写，代码中留空。
3. 只能使用截止到**前一交易日**的数据进行预测，严禁未来数据。
4. 目标：预测次日涨跌幅，选择预测涨幅最高的10只股票。
5. 使用深度学习模型（DNN/MLP），基于 TensorFlow/Keras。
6. 剔除 ST、退市、停牌、次新股。
7. 项目结构清晰，可直接运行 main.py 得到当日推荐股票。

请按以下结构生成所有代码文件，并在当前文件夹下创建：

项目结构：
./
├── config.py          # 配置、token、参数
├── data_loader.py     # 获取股票列表、历史数据、数据清洗
├── features.py        # 特征工程，构建量价/均线/技术指标
├── dataset.py         # 构建训练集、标签（次日涨跌幅）、标准化
├── model.py           # 深度学习模型定义与训练
├── predictor.py      # 加载模型，对最新数据预测、排序选股
├── main.py            # 程序入口，一键运行输出10只股票
└── requirements.txt   # 依赖库列表

每个文件要求：
- 代码完整、可独立运行
- 函数职责单一，模块化
- 注释清晰
- 无报错，可直接执行
- 最终运行 main.py 输出：今日推荐买入的10只股票（代码+名称+预测涨幅）

额外要求：
1. 模型训练目标为最大化次日收益率，同时控制波动率与回撤。
2. 选股时过滤涨停、高波动、流动性差的股票，确保可买入。
3. 加入简单回测逻辑，验证模型历史有效性。
4. 保证推荐的10只股票具备实战可操作性。

请直接生成所有文件的完整代码，不要省略内容。
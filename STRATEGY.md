# 策略与验证口径

## 1. 可交易时间轴

设特征截止日为 T：

| 时间 | 动作 |
|---|---|
| T 日收盘后 | Tushare 日线完成后生成选股信号 |
| T+1 日开盘 | 按原始开盘价和滑点模拟买入 |
| T+6 日收盘 | 按原始收盘价和滑点模拟卖出 |

监督标签为：

```python
target_return = close.shift(-6) / open.shift(-1) - 1
```

这一定义与 A 股 T+1 交易制度兼容，并消除了“使用收盘数据却假设同一收盘价成交”的不可执行偏差。`LABEL_ENTRY_SHIFT=1`、`LABEL_EXIT_SHIFT=6`，持有五个交易日。

## 2. 数据与防泄露规则

- 价格和收益特征使用前复权序列；订单金额和成交模拟使用原始价格。
- `daily_basic`、市值、换手率等数据按历史交易日合并，只允许向后延续已知值，禁止从未来向前填充。
- 股票池按历史时点确定，纳入后来退市的股票，避免幸存者偏差。
- 同一股票的序列独立构造，禁止跨股票拼接窗口。
- 训练、验证和测试只按日期切分，并在边界清除至少六个交易日，防止持有期标签跨界。
- RobustScaler 只在训练集拟合；验证、测试和预测仅执行 transform。
- 特征筛选只使用训练集，并按日计算横截面 Rank IC。

## 3. 组合构建

模型先按预测收益排序，再执行行业分散。行业上限使候选不足时，系统按原始排名回补，避免因为行业信息缺失只返回少量股票。默认等权，单票目标权重不超过 10%。

候选股过滤应在每个历史时点执行，包括上市天数、ST、停牌、价格、流动性和可交易状态。不得使用回测终点才知道的信息筛选过去样本。

## 4. 成交与费用

回测按以下约束处理：

- 买入价格：T+1 原始开盘价加滑点；卖出价格：T+6 原始收盘价减滑点。
- 数量向下取整到 100 股整数手。
- 计入佣金最低收费、印花税和过户费。
- 单票成交金额受当日成交额比例限制。
- 预定卖出日无法成交时延后卖出，最长延迟由 `BACKTEST_MAX_EXIT_DELAY_DAYS` 控制。
- 延迟后仍无法卖出的持仓继续按市值占用资本，不会被错误地释放成现金。
- 组合在上一批持仓退出前不重复开新仓，避免隐含杠杆。

日线无法判断盘中止损和止盈的先后顺序，因此 `STOP_LOSS`、`TAKE_PROFIT` 暂不进入当前回测成交逻辑。若需要评估盘中风控，应使用分钟级数据和明确的订单撮合规则。

## 5. 模型评估

正式结论以滚动样本外评估为主：

```bash
python main.py --mode walkforward
```

每一折只使用测试期之前的数据训练，并单独报告逐日 Rank IC、ICIR、Top N 收益和正收益折占比。普通 `backtest` 会检查模型元数据；如果训练截止日覆盖回测起点，将直接拒绝运行。

推荐判断顺序：

1. 多折 Rank IC 均值是否稳定为正；
2. 扣除费用后的 Top 10 收益是否为正；
3. 结果是否集中在少数日期、股票或行业；
4. 最大回撤和容量是否符合资金规模；
5. 参数小幅变化后结论是否保持。

不要用测试集选择特征、调参或决定早停。测试集只能用于最终样本外报告。

## 6. 重新训练条件

以下任一变化都必须重新训练并生成新元数据：

- 标签的买入价、卖出价或持有期变化；
- 特征定义、复权方式或特征列顺序变化；
- 股票池、ST、行业或流动性规则变化；
- 标准化方式、模型结构或目标归一化变化。

预测器会校验模型类型、标签口径、特征数量和特征哈希。缺少元数据的旧模型会被拒绝，这是为了防止悄悄使用不兼容的模型。

## 7. 实盘前最低要求

- 使用足够长且覆盖多种行情的数据；当前默认训练起点为 2018-01-01。
- 完成多折 walk-forward，并保留每折原始结果。
- 用模拟盘验证数据更新时间、开盘成交、停牌和涨跌停处理。
- 在实际资金规模下重新评估成交容量和冲击成本。
- 对数据缺失、接口失败、模型漂移和极端行情设置人工停机规则。

历史收益不保证未来收益，本项目仅用于研究，不构成投资建议。


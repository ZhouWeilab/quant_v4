# 模型架构升级说明

## 为什么升级到 CNN + LSTM + Attention？

### A 股超短线交易的挑战

1. **高噪声环境**
   - 市场情绪波动大
   - 散户行为不理性
   - 消息面影响剧烈

2. **微弱信号**
   - 超短线收益率通常只有 1-3%
   - 信号被噪声淹没
   - 难以提取有效特征

3. **非平稳性**
   - 市场规律不断变化
   - 历史模式失效快
   - 需要自适应能力

4. **T+1 约束**
   - 今日买入次日才能卖出
   - 只能使用历史数据
   - 预测窗口固定

---

## 架构对比

### 1️⃣ 原始 DNN 模型

```
输入：单日特征向量 (N维)
    ↓
全连接层 × 4
    ↓
输出：次日涨跌幅
```

**优点**：
- ✅ 简单快速
- ✅ 易于训练
- ✅ 计算量小

**缺点**：
- ❌ 无法捕捉时序依赖
- ❌ 丢失历史信息
- ❌ 特征权重固定
- ❌ 对微弱信号不敏感

**适用场景**：
- 简单预测任务
- 数据量小
- 不关注时序

---

### 2️⃣ CNN + LSTM + Attention 混合架构

```
输入：时序窗口 (20天 × N特征)
    ↓
┌─────────────────────┐
│   CNN 层             │  ← 提取局部模式
│  - 识别连续上涨     │
│  - 识别量价配合     │
│  - 识别K线形态      │
└─────────────────────┘
    ↓
┌─────────────────────┐
│   LSTM/GRU 层        │  ← 捕捉长期依赖
│  - 记忆历史趋势     │
│  - 理解序列关系     │
│  - 双向信息流       │
└─────────────────────┘
    ↓
┌─────────────────────┐
│   Attention 层       │  ← 学习重要权重
│  - 关注关键时刻     │
│  - 关注重要特征     │
│  - 多头捕捉模式     │
└─────────────────────┘
    ↓
全连接层 × 3
    ↓
输出：次日涨跌幅
```

**优点**：
- ✅ 捕捉时序依赖关系
- ✅ 提取局部特征模式
- ✅ 自动学习特征权重
- ✅ 多尺度特征融合
- ✅ 对微弱信号更敏感
- ✅ 更强的泛化能力

**缺点**：
- ❌ 训练时间较长
- ❌ 计算资源要求高
- ❌ 模型复杂度高
- ❌ 过拟合风险增加

**适用场景**：
- 时序预测任务
- 高噪声环境
- 微弱信号提取
- 有足够数据和算力

---

## 各模块的作用

### 1. CNN 模块：局部特征提取器

**作用**：识别短期技术形态

```python
# 例如识别：
- 连续3天上涨 + 量能放大 → 多头趋势
- 收盘价连续突破均线 → 突破信号
- 上影线长 + 成交量大 → 上方压力
```

**优势**：
- 平移不变性：模式可以出现在任何时间点
- 局部感受野：关注短期连续变化
- 参数共享：减少参数量，防止过拟合

**配置建议**：
```python
CNN_FILTERS = [64, 128, 128]  # 逐层增加提取更抽象特征
CNN_KERNEL_SIZES = [3, 3, 3]  # 3天窗口捕捉短期模式
```

---

### 2. LSTM/GRU 模块：序列记忆器

**作用**：理解长期趋势和转折点

```python
# 例如记忆：
- 20天前开始的上涨趋势
- 10天前的突破后调整
- 5天前的成交量异常
```

**LSTM vs GRU**：

| 特性 | LSTM | GRU |
|------|------|-----|
| 门控机制 | 3个门 | 2个门 |
| 参数量 | 多 | 少 |
| 训练速度 | 慢 | 快 |
| 长期记忆 | 强 | 中等 |
| 适用场景 | 长序列 | 短序列 |

**双向 RNN**：
```python
BIDIRECTIONAL = True

# 正向：看过去 (t-20 → t-1)
# 反向：看"未来"序列内 (t-1 → t-20)
# 融合：综合判断
```

---

### 3. Multi-Head Attention：智能权重分配器

**作用**：自动识别重要特征和时刻

```python
# 例如学习到：
- 昨日的成交量比10天前更重要 (时间权重)
- MACD 比 MA5 更重要 (特征权重)
- 在上涨趋势中，RSI > 70 是重要信号
```

**多头机制**：

```
Head 1: 关注价格变化
Head 2: 关注成交量
Head 3: 关注技术指标
...
Head 8: 关注量价关系

融合 → 综合判断
```

**自注意力优势**：
- 全局感受野：可以关注任意时间点
- 动态权重：根据输入自适应调整
- 可解释性：可以可视化注意力权重

---

## 损失函数升级

### 原始：简单 MSE

```python
Loss = MSE(y_true, y_pred)
```

**问题**：
- 只关注绝对误差
- 不关心方向
- 不关心排序

### 升级：组合损失

```python
Loss = α × MSE + β × Direction + γ × Ranking
```

#### 1. MSE Loss（准确性）
```python
MSE = mean((y_true - y_pred)²)
```
- 预测值接近真实值

#### 2. Direction Loss（方向性）
```python
Direction = -mean(sign(y_true) × sign(y_pred))
```
- 预测涨跌方向正确
- 对超短线更重要（涨 1% 和涨 2% 都是"涨"）

#### 3. Ranking Loss（排序性）
```python
# 确保高收益股票排在前面
for 股票i, 股票j:
    if 真实收益i > 真实收益j:
        希望 预测收益i > 预测收益j
```
- 相对排序比绝对预测更重要
- 选股本质是排序问题

**权重配置**：
```python
LOSS_MSE_WEIGHT = 1.0
LOSS_DIRECTION_WEIGHT = 0.5  # 方向很重要
LOSS_RANKING_WEIGHT = 0.3    # 排序也重要
```

---

## 数据处理升级

### 1. 时序窗口构建

**原始**：单日特征
```python
X = [特征1, 特征2, ..., 特征N]  # 1天
y = T+1 开盘到 T+6 收盘的五交易日收益率
```

**升级**：滑动窗口
```python
X = [
    [Day1: 特征1, 特征2, ..., 特征N],
    [Day2: 特征1, 特征2, ..., 特征N],
    ...
    [Day20: 特征1, 特征2, ..., 特征N]
]  # 20天
y = Day20 收盘后生成信号，下一交易日开盘到第六交易日收盘的收益率
```

**优势**：
- 包含历史信息
- 捕捉趋势变化
- 提供时序上下文

---

### 2. 标准化方法

**原始**：StandardScaler
```python
X_scaled = (X - mean) / std
```
- 对异常值敏感
- 金融数据常有极端值

**升级**：RobustScaler
```python
X_scaled = (X - median) / IQR
```
- 基于中位数和四分位数
- 对异常值鲁棒
- 更适合金融数据

---

## 评估指标升级

### 原始指标

```python
- MAE: 平均绝对误差
- RMSE: 均方根误差
```

### 升级指标

```python
1. Direction Accuracy: 方向准确率
   - 涨跌方向预测正确的比例
   - 对超短线更重要

2. IC (Information Coefficient): 信息系数
   - 预测值与真实值的相关性
   - 量化策略的标准指标

3. Quantile Returns: 分位数收益
   - 将股票按预测收益分成5组
   - 检查 Top 20% 是否真的收益最高
   
4. Sharpe Ratio: 夏普比率
   - 收益 / 波动率
   - 风险调整后收益
```

**示例输出**：
```
验证集评估:
loss: 0.0012
mae: 0.0234
rmse: 0.0345
direction_accuracy: 0.5823  ← 58.23% 方向正确
correlation: 0.1245         ← IC = 0.12（不错）
ic: 0.1245
quantile_returns: ['0.0012', '0.0089', '0.0145', '0.0198', '0.0287']
                   ↑ 最低20%  ↑ 中间    ↑ 最高20%
top_quantile_return: 0.0287  ← Top 20% 平均收益 2.87%
bottom_quantile_return: 0.0012
```

---

## 训练策略优化

### 1. 早停机制

```python
EARLY_STOPPING_PATIENCE = 15

# 如果验证集 loss 连续 15 轮没有改善，停止训练
# 并恢复最佳权重
```

### 2. 学习率衰减

```python
REDUCE_LR_PATIENCE = 7

# 如果验证集 loss 连续 7 轮没有改善
# 学习率 × 0.5
```

### 3. 批次大小

```python
BATCH_SIZE = 64  # 时序模型建议较小

# 更小的 batch：
# - 梯度更新更频繁
# - 更好的泛化能力
# - 更稳定的训练
```

---

## 实际效果预期

### 性能提升

| 指标 | 原始 DNN | CNN+LSTM+Attention | 提升 |
|------|----------|-------------------|------|
| 方向准确率 | 52% | 58% | +6% |
| IC | 0.05 | 0.12 | +140% |
| Top 20% 收益 | 1.2% | 2.3% | +92% |
| 夏普比率 | 0.8 | 1.5 | +88% |

*注：以上为理想情况，实际效果受多种因素影响*

### 训练时间对比

| 模型 | CPU 训练 | GPU 训练 |
|------|---------|---------|
| 原始 DNN | 5 分钟 | 2 分钟 |
| CNN+LSTM+Attention | 30 分钟 | 5 分钟 |

**建议**：
- 有 GPU：首选 CNN+LSTM+Attention
- 无 GPU：可先用 DNN 快速验证，再用升级版

---

## 何时使用哪种架构？

### 使用简单 DNN 的情况

✅ 数据量小（< 1000 只股票）
✅ 快速验证想法
✅ 计算资源有限
✅ 不关注时序关系

### 使用 CNN+LSTM+Attention 的情况

✅ 数据量充足（> 3000 只股票 × 1年+）
✅ 需要捕捉时序依赖
✅ 追求更高准确率
✅ 有 GPU 资源
✅ 愿意投入更多训练时间

---

## 进一步优化方向

### 1. 模型集成
```python
# 训练多个模型并投票
Model 1: CNN + LSTM + Attention (seed=42)
Model 2: CNN + GRU + Attention (seed=123)
Model 3: Transformer (seed=456)

Final = Ensemble(Model1, Model2, Model3)
```

### 2. 特征工程
```python
# 添加更多特征
- 资金流向数据
- 情绪指标
- 宏观经济数据
- 行业轮动信息
```

### 3. 在线学习
```python
# 定期使用最新数据更新
每周重训练
或使用增量学习（Transfer Learning）
```

### 4. 市场环境自适应
```python
# 不同市场环境使用不同策略
牛市模型 + 熊市模型 + 震荡市模型
根据市场状态自动切换
```

---

## 总结

CNN + LSTM + Attention 架构专为 A 股超短线量化交易设计：

✅ **CNN** 提取局部特征模式（技术形态）
✅ **LSTM/GRU** 捕捉时序依赖关系（趋势变化）
✅ **Attention** 自动学习特征权重（重要性）
✅ **组合损失** 强化方向性和排序能力
✅ **时序窗口** 提供历史上下文信息
✅ **RobustScaler** 对异常值鲁棒

这些改进使模型能够在高噪声、微弱信号、非平稳的 A 股市场中，提取有效信号，做出更准确的预测。

---

**Happy Trading! 📈**
